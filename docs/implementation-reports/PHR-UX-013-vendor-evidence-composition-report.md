# PHR-UX-013 — Vendor Evidence Composition Restoration Implementation Report

Date: 2026-08-07

Vendor Workspace now presents the selected raw-card markets in the intended decision order. TCGplayer values and the exact applicable regional provider share one `Raw-card market evidence` card. Magic labels and loads LigaMagic; English Pokémon labels and loads LigaPokémon. Unsupported catalogues make no Liga claim.

The regional repository now has one provider-aware read model over the promoted operational tables. It continues to require exact `MATCHED` crosswalk rows, orders candidates by newest observation and reconciliation, and returns allowlisted provider, source-run, condition, and language provenance. The production database confirms LigaMagic run `dry-run-20260730T203243818Z` is the newest complete last-good Magic snapshot and LigaPokémon run `dry-run-20260805T070105248Z` is the promoted Pokémon snapshot.

PriceCharting is restored as a closed-by-default disclosure immediately after the combined card. It performs no lookup before expansion, resets when the exact selection changes, and contains the existing certificate lookup so the secondary graded workflow is not duplicated elsewhere. Regional evidence was removed from the separate Buying decision panel.

The private production service behind `https://ramons-mac-studio.tailaa2d39.ts.net:9444` was rebuilt and restarted. All 461 tests, standalone TypeScript, warning-free lint, production build, diff hygiene, live Magic/Pokémon API probes, live selection switching, collapsed/expanded PriceCharting behavior, and narrow-column overflow/touch-target checks pass. No acquisition, reauthentication, crosswalk rebuild, provider mutation, transaction, or public deployment occurred.
