# PHR-UX-007 — Mobile Pricing Lookup

- Added an executable completed-generation gate for the exact zoom and web semantic payloads required by Designer direction, plus sanitized tests proving legacy count-only zoom and raw `StaticText` evidence are rejected. The existing generation remains unchanged and nonconforming.

- Preserved the completed pricing and accessibility work after isolating the latest compact-native failure to leaked system VoiceOver state. Jarvis bridge v34 now owns a clean serialized rerun with VoiceOver verified OFF around every scenario; no new customer-facing defect or product change was established.

- Repaired compact-iPhone evidence interaction after short drags left the sealed expander off-screen. The non-shipping test host now exposes a genuine 44-point expander target and uses a settled bounded drag inside its scroll view; product behavior is unchanged and fresh Worker validation remains required.

- Repaired incomplete spoken result semantics: web results now expose product identity, delivered/listing/shipping state, history, and snapshot date in one explicit summary; native evidence keeps those facts separately traversable and requires a complete genuine VoiceOver transcript. Fresh Worker evidence remains required.

- Reconciled the serialized Worker's universal web accessibility probe with the repository target contract. Price Lookup now exposes a labelled accessibility region, and evidence is scoped to that region by backend-node identity and accessibility descendants.

- Corrected the empty web semantic-capture contract after fresh evidence returned zero accessibility nodes. The Worker must now query the unique `.pricing-lookup` subtree through the page-target accessibility session, record capture provenance, and reject empty output. Product behavior is unchanged; fresh Worker evidence remains required.
- Removed the Next.js development indicator from the isolated evidence runtime after it obscured narrow-layout controls and result text, and aligned the stale fixture's result snapshots with its category freshness date. Fresh serialized Worker rendering remains required.
- Repaired the serialized web semantic-evidence handoff after Designer review found global application chrome in a purportedly scoped artifact. The contract now requires a unique `.pricing-lookup` backend node, a scoped accessibility query, serialized root provenance, and `Price lookup` as the first derived spoken node. Product behavior is unchanged; fresh Worker evidence remains required.
- Tightened the serialized evidence contract after policy-v31 review: zoom reports now require complete control/identity/intersection measurements, web reading order must exclude development tooling and duplicate raw descendants, and search/condition controls expose stable explicit accessible names. Runtime conformance remains pending a fresh Mac Worker generation.

## Browser-Zoom Reflow Remediation — 2026-07-28

- Structurally removed sticky classes from both search and the Singles condition panel during 200%/400% zoom reflow, while preserving ordinary phone stickiness.
- Required fresh 200%/400% diagnostics to prove sticky search and unobscured result identity together.
- Replaced pointer-emulation-dependent Worker reflow with explicit 200%/400% evidence URLs; only the condition panel reflows and search remains sticky.
- Prevented the sticky Singles condition panel from covering the first result's identity at 200% and 400% desktop browser zoom.
- Added a fine-pointer CSS reflow fallback for the Worker's 640px and 320px desktop zoom viewports after its runtime did not expose the prior script zoom heuristic.
- Preserved sticky condition behavior on coarse-pointer phone layouts.
- Strengthened the Mac Worker contract so zoom evidence must prove readable result identity, not merely the absence of horizontal overflow.
- Added stable zoom probe hooks and explicit computed-position, non-overlap, visibility, and overflow pass criteria for fresh Worker evidence.
- Removed the sticky class itself during zoom reflow and added a machine-readable reflow marker, closing the overlay classification found by fresh 200%/400% host diagnostics.

## 2026-07-28 Web Environment Evidence Readiness

- Added explicit reduced-motion and increased-contrast behavior to the web lookup.
- Strengthened the Worker contract to require distinct web diagnostics plus screenshots for both environments and web-scoped manifest results for contrast.
- Added a deterministic regression guard for the CSS and target contract.
- Runtime conformance remains blocked on a fresh coherent serialized Mac Worker generation and matching authority receipt.

## 2026-07-28 Global Focus Remediation

- Added an opaque cyan focus outline and border to the global search and user-menu controls after fresh host evidence found no visibly distinguishable indicator.
- Added a deterministic preflight guard for both focus treatments.
- Runtime visible-focus conformance remains pending a fresh coherent Mac Worker generation.

## 2026-07-28 Runtime-Evidence Remediation

- Raised the contrast of the command-palette labels rendered alongside Price Lookup evidence.
- Added Space-key operation for Price Lookup disclosure summaries.
- Required non-null contrast, keyboard-operation, and visible-focus diagnostics from the serialized Mac Worker.
- Runtime conformance remains pending a fresh coherent Worker generation.

Status: implemented on 2026-07-28; awaiting fresh coherent Mac Worker evidence, conformance review, and real export binding.

The native evidence disclosure now behaves as a reversible expander and its UI test re-resolves accessibility state after SwiftUI inserts the third sealed card. This repairs the fresh `testSealedExpansion` failure without changing the web product scope; the serialized Mac Worker must still establish runtime conformance.

The follow-up native evidence repair also discards the element snapshot captured before scrolling, verifies a freshly resolved collapsed disclosure immediately before activation, and independently queries the expanded state afterward. Product behavior is unchanged; fresh serialized runtime evidence is still required.

Adds the documented foundation for a phone-first English Pokémon price lookup: unified singles/sealed imports, explicit delivered-price uncertainty, condition-wide singles repricing, category freshness, change-only history, market movement, relevance-based grouping, and optional asking-price comparison. Production import activation remains gated on an authoritative sanitized Pricing Tool schema; deployment and durable hosting remain outside scope.

Fresh host-preflight remediation adds a deterministic target contract, a local Next.js evidence entry point with seeded sealed-plus-singles, single-specific, missing-price, stale, and error scenarios, and a non-shipping executable iOS app/UI-test host for serialized Mac Worker capture. Local rendering no longer depends on fetching Google Fonts. This evidence infrastructure does not add fixture data or a native application to product scope.

Debugger recovery now pins the evidence-only development server to Webpack and gives the isolated Mac Worker an explicit readiness probe before capture. This avoids the previously observed default-bundler port-bind path without changing the customer-facing Pricing Lookup.

The earlier empty-capture result was traced to policy-v10 Worker classification before safe runtime-target discovery. The repository contract and route are intact, and a later isolated smoke server returned `200`. Later policies reached the declared targets; current conformance depends on replacing the rejected v14 generation described below.

Policy-v14 evidence was rejected because a referenced artifact hash did not match, the condition selector's focus artifact contradicted the manifest, and no hashed web screen-reader artifact supported the web pass. The selector now has an explicit focus outline, and the Worker target contract requires separate hashed web focus and screen-reader artifacts. A fresh serialized generation is required; the rejected artifacts are not release evidence.

Fresh host remediation adds a uniform Price Lookup focus-visible fallback, including forced-colors behavior, and requires separate diagnostic-bearing keyboard-operation and visible-focus artifacts. Repository checks pass; runtime conformance still requires a fresh serialized Worker generation.

The latest bounded Designer remediation restores the two-item compact sealed preview, keeps operational errors distinct from empty loaded-category results, adds an explicit opaque focus indicator to the search control, and requires a representative baseline large-iPhone capture separate from the special Dynamic Type/increased-contrast run.

The latest bounded evidence-host repair makes sealed expansion reliable across accessibility layouts by scrolling the actual identified button until hittable and waiting for that same element's expanded accessibility state. Runtime conformance remains unclaimed until the serialized Mac Worker reruns it.
