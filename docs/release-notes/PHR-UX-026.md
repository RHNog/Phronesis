# PHR-UX-026 Release Notes

Added the normalized multi-region and append-only correction foundation with full-frame fallback, bounds/rotation validation, deterministic ordering, and immutable parent revisions. Automatic binder segmentation remains deferred until a labeled benchmark exists.

The 2026-08-05 benchmark increment adds a local Apple Vision rectangle-suggestion command, top-left normalized output, deterministic reading order, overlapping-duplicate and containing-page suppression, a strict TypeScript worker boundary, and SHA-256-sealed IoU benchmark reporting. Synthetic fixtures cannot qualify production. A real edge-to-edge card smoke test selected an internal rectangle, confirming that automatic adoption must remain disabled until a representative labeled binder holdout passes.
